# eHub.Contracts

Intentionally **empty** for now.

Feature DTOs and request/response types live next to vertical slices in **`eHub.Application`** (feature ownership).

Use this project later for:

- Public integration contracts shared with external systems
- Versioned event schemas published outside the monolith

Do not dump internal API DTOs here.
